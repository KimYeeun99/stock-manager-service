spring:
  config:
    import:
      - classpath:mysql-config/application.yml
      - classpath:http-application.yml
  application:
    name: "project"
  messages:
    encoding: UTF-8

management:
  endpoints:
    web:
      exposure:
        include: "*"