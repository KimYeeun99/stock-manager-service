project(':stock-application') {
    dependencies {
        implementation project(':stock-common')
    }

    tasks.bootJar {
        enabled = false
    }

    tasks.jar {
        enabled = true
    }
}